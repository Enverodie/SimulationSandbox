function handleKeypress(key) {
    switch(key) {
        case 'r': // reset
            break;
        case 's': // start
            break;
        case 'p': // pause
            break;
        default:
            break;
    }
}

function handleClick(event) {
    
}